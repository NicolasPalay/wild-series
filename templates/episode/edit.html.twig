{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block content %}
    <section class="container mt-5 mb-4">
        <h1>Edit Episode</h1>
        <article class="listAllCategory m-auto mb-5 p-2">
            {{ include('episode/_form.html.twig', {'button_label': 'Update'}) }}

            <a class="mb-md-5 d-block text-center" href="{{ path('app_episode_index') }}">back to list</a>

            {{ include('episode/_delete_form.html.twig') }}
        </article>
    </section>
{% endblock %}
